---
title: MCP tools
description: Parameters and behavior for every CostMCP MCP tool.
---

Call these via local stdio or remote HTTP at [`https://mcp.costmcp.com`](/mcp/remote-http).

## `log_usage`

Permission: `log_usage`

| Param | Type | Required |
|-------|------|----------|
| `project` | string | ✓ |
| `provider` | string | ✓ |
| `unit_type` | string | ✓ |
| `quantity` | number (>0) | ✓ |
| `model` | string | |
| `estimated_cost` | number | |
| `feature` | string | |
| `batch_id` | string | |

**Local:** `POST /api/v1/messages` with `source: "mcp"`.  
**Remote:** persists directly via service role.

## `add_expense`

Permission: `add_expenses`

| Param | Type | Required | Default |
|-------|------|----------|---------|
| `project` | string | ✓ | |
| `vendor` | string | ✓ | |
| `amount` | number (>0) | ✓ | |
| `currency` | string | | `USD` |
| `category` | string | | |
| `notes` | string | | |
| `timestamp` | ISO datetime | | now |

Refunds: set `expense_type` to `refund` (or send a negative amount). Amounts in other currencies are converted to USD via `fx_rates`.

## `add_subscription`

Permission: `manage_subscriptions`

| Param | Type | Required | Default |
|-------|------|----------|---------|
| `project` | string | ✓ | |
| `vendor` | string | ✓ | |
| `amount` | number (≠0) | ✓ | |
| `interval` | string | ✓ | |
| `currency` | string | | `USD` |
| `category` | string | | |
| `notes` | string | | |
| `status` | string | | `active` |
| `timestamp` | ISO datetime | | now |

## `create_project`

Permission: `manage_projects`

| Param | Type | Required | Default |
|-------|------|----------|---------|
| `slug` | string | ✓ | |
| `name` | string | ✓ | |
| `description` | string | | |
| `budget` | number (≥0) | | |
| `currency` | string | | `USD` |
| `environment` | `development` \| `staging` \| `production` \| `other` | | `production` |

Creates a project explicitly with name, budget, and metadata. Returns `409` if the slug already exists (unlike implicit creation via `log_usage`, which upserts silently).

**Local:** `POST /api/v1/projects`.  
**Remote:** DB insert via service role.

## `get_project_spend`

Permission: `read_summaries`

| Param | Type | Required |
|-------|------|----------|
| `project` | string | ✓ |
| `from` | ISO datetime | |
| `to` | ISO datetime | |

**Local:** `GET /api/v1/projects/{project}/spend`.  
**Remote:** DB query (limit 200).

## `get_budget_status`

Permission: `read_summaries`  
No parameters. Returns the same shape as `GET /api/v1/budgets/status`.

## `get_monthly_summary` (remote only)

Permission: `read_summaries`  
No parameters. Same shape as `GET /api/v1/summary/month`.

```json
{
  "period": "month",
  "total_usd": 0,
  "usage_usd": 0,
  "subscription_usd": 0,
  "expense_usd": 0,
  "by_project": {},
  "top_project": null,
  "message_count": 0
}
```

## Example prompts

- "Create a project called Slideshow Studio with slug slideshow-studio and a $200 monthly budget"
- "Log 1.2M tokens under ProgressGoat"
- "Log $18.40 Kling batch under YouTube Pipeline"
- "How much did Slideshow Studio cost this week?"
- "Can I afford 100 video generations?"
