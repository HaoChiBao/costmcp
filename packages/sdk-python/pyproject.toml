[project]
name = "costmcp"
version = "0.0.1"
description = "CostMCP Python SDK — AI-native cost ledger client"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["httpx>=0.28"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/costmcp"]
